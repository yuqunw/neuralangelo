# conda env create --file neuralangelo.yaml
name: neuralangelo
channels:
  - conda-forge
  - pytorch
dependencies:
  # general
  - gpustat
  - gdown
  - cmake
  # python general
  - python=3.8
  - pip
  - numpy
  - scipy
  - ipython
  - jupyterlab
  - cython
  - ninja
  - diskcache
  # pytorch
  - pip:
    - -r requirements.txt
